error: a `fan_out` binding cannot be returned as the workflow's value (the raw Argo aggregate's elements are individually JSON-encoded, so the parent would read a double-encoded array). Pass the binding to a consuming template inside this workflow and return that instead.
  --> tests/ui/wf_if_arm_return_fan_out.rs:16:18
   |
16 |       if count > 3 {
   |  __________________^
17 | |         let xs = make();
18 | |         xs.fan_out(|x| caps(x))
19 | |     } else {
   | |_____^
