error: macros aren't lowered to workflow steps. A macro call here would be dropped from the DAG. If you need pod resources (`host!`, `secret!`, `load_artifact!`, `save_artifact!`), declare them inside a #[container] body; only container bodies actually run.
 --> tests/ui/wf_macro_stmt.rs:5:5
  |
5 |     println!("hello");
  |     ^^^^^^^^^^^^^^^^^^
