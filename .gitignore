# Rust build artifacts
/target

# Backup files from rustfmt / rustc
**/*.rs.bk
*.pdb

# direnv / nix
.direnv/
result
result-*

# mdBook render output (docs/ source is tracked; the build is not)
docs/book/
